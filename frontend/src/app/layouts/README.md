# Layouts

Los layouts son la estructura base de las páginas.

- Public Layout → Landing, Login y Registro.
- Club Layout → Usuario autenticado.
- Admin Layout → Panel de administración.

Dentro de cada layout luego habrá un componente Angular que contendrá el navbar, router-outlet, footer, etc.